package operator;

import item.Office;

public class CleanOffice {

	/**
	 * Robot cleans office o
	 * Pre: robot-location(o), dirty(o), empty(o)
	 * Add: clean(o)
	 * Delete: dirty(o)
	 * @param o
	 */
	public CleanOffice(Office o){
		
	}
}
